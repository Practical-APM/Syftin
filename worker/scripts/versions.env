# Syftin contributor node — pinned versions (sourced by install scripts)
SYFTIN_VERSION="${SYFTIN_VERSION:-0.1.0}"
PLAYWRIGHT_GO_VERSION="${PLAYWRIGHT_GO_VERSION:-v0.5700.1}"
PLAYWRIGHT_DRIVER_VERSION="${PLAYWRIGHT_DRIVER_VERSION:-1.57.0}"
CHROMIUM_REVISION="${CHROMIUM_REVISION:-1200}"
